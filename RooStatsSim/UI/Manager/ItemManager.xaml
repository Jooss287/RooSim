<Page x:Class="RooStatsSim.UI.Manager.ItemManager"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RooStatsSim.UI.Manager"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      Title="ItemManager">
    <Page.Resources>
        <local:ItemListBox x:Key="ItemList"/>
    </Page.Resources>
    <StackPanel Orientation="Vertical" Margin="0,0,0,0">
        <StackPanel Orientation="Horizontal">
            <Button Content="New DB" Width="100" Height="30" TabIndex="9"
                    Margin="5,5,5,5"
                    Click="New_DB_Click"
                    VerticalAlignment="Top" HorizontalAlignment="Left"/>
            <Button Content="Add DB" Width="100" Height="30" TabIndex="8"
                    Margin="5,5,5,5"
                    Click="Add_DB_Click"
                    VerticalAlignment="Top" HorizontalAlignment="Left" />
            <ComboBox Name="cmb_item_type" Width="100" Height="30"
                      Margin="5,5,5,5"
                      VerticalAlignment="Top" HorizontalAlignment="Left"
                      SelectionChanged="cmb_item_type_SelectionChanged">
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <ListBox x:Name="DB_ListBox" Width="200"
                     ItemsSource="{Binding Source={StaticResource ItemList}}"
                     SelectionChanged="DB_ListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=Name}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <GroupBox Header="DB Info" >
                <StackPanel Orientation="Horizontal">
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" >
                            <TextBox x:Name="Item_id" Text="{Binding Path=Id}" TextAlignment="Center"
                                     Width="40" Height="22" IsEnabled="False" Margin="10,0,0,0" VerticalAlignment="Bottom"/>
                            <TextBox x:Name="Item_name" Text="{Binding Path=Name}"
                                     Style="{Binding Source={StaticResource MaterialDesignFloatingHintTextBox}}"
                                     materialDesign:HintAssist.Hint="아이템 이름" Width="150" Margin="10,0,0,0" />
                            <ComboBox Name="cmb_equip_type" SelectionChanged="cmb_equip_type_SelectionChanged"
                                      VerticalAlignment="Bottom" Margin="10,0,0,0" />
                            <TextBox x:Name="Item_Level" Text="{Binding Path=LevelLimit}"
                                     Style="{Binding Source={StaticResource MaterialDesignFloatingHintTextBox}}"
                                     materialDesign:HintAssist.Hint="레벨 제한" Width="60" Margin="10,0,0,0" />
                            <TextBox x:Name="Item_CardSlot" Text="{Binding Path=CardSlot}"
                                     Style="{Binding Source={StaticResource MaterialDesignFloatingHintTextBox}}"
                                     materialDesign:HintAssist.Hint="카드 슬롯" Width="60" Margin="10,0,0,0" />
                            <TextBox x:Name="Item_EnchantSlot" Text="{Binding Path=EnchantSlot}"
                                     Style="{Binding Source={StaticResource MaterialDesignFloatingHintTextBox}}"
                                     materialDesign:HintAssist.Hint="인챈트 개수" Width="70" Margin="10,0,0,0"/>
                        </StackPanel>
                        <StackPanel>
                            <ListBox x:Name="list_Job_limit" Visibility="Visible">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Style="{StaticResource MaterialDesignFilterChipPrimaryCheckBox}"
                                                  Content="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}"
                                                  IsChecked="{Binding Path=Point, UpdateSourceTrigger=PropertyChanged}"
                                                  Click="CheckBox_Click"
                                                  HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Tag ="ITYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_Ioption" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="증감값"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" MinWidth="100">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_iOption"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            <StackPanel Tag="DTYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_Doption" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="%값"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_dOption"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            

                            <StackPanel Tag="IFTYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_IFoption" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="Ex)힘10"/>
                                    <TextBlock Text="당" VerticalAlignment="Bottom" Margin="5,0,0,3" HorizontalAlignment="Center"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="증가량"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_ifOption"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <StackPanel Tag="SE_ATTACKRATE_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_se_attackrate_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="상태이상저항(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_attackrate_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            <StackPanel Tag="SIZE_INC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_size_inc_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="크기증가데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_size_inc_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            <StackPanel Tag="TRIBE_INC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_tribe_inc_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="종족증가데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_tribe_inc_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>

                            <StackPanel Tag="MOBTYPE_INC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_mobtype_inc_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="스타일증가데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_mobtype_inc_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <StackPanel Tag="SE_REGISTANCE_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_se_registance_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="상태이상저항(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_se_registance_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            <StackPanel Tag="SIZE_DEC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_size_dec_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="크기감소데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_size_dec_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            <StackPanel Tag="TRIBE_DEC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_tribe_dec_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="종족감소데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_tribe_dec_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>

                            <StackPanel Tag="MOBTYPE_DEC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_mobtype_dec_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="스타일감소데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_mobtype_dec_option" 
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <StackPanel Tag ="ELEMENT_DAMAGE_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_element_damage_option" SelectedIndex="0"
                                              SelectionChanged="cmb_element_damage_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="속성 데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_element_damage_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            <StackPanel Tag ="ELEMENT_INC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_element_inc_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="몬스터(속성)에게 증가데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_element_inc_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                            <StackPanel Tag ="ELEMENT_DEC_TYPE"
                                        Style="{Binding Source={StaticResource ItemManagerListStackPanel}}">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <ComboBox Name="cmb_element_dec_option" SelectedIndex="0"
                                              SelectionChanged="cmb_option_SelectionChanged" VerticalAlignment="Bottom"/>
                                    <TextBox Style="{Binding Source={StaticResource ItemManagerValueBox}}"
                                        materialDesign:HintAssist.Hint="속성감소데미지(%)"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseUp}"
                                            Click="Del_Option_Click"/>
                                    <Button Style="{Binding Source={StaticResource Style_Btn_UpDownload}}"
                                            Content="{materialDesign:PackIcon ArrowCollapseDown}"
                                            Click="Add_Option_Click"/>
                                </StackPanel>
                                <ListBox x:Name="list_element_dec_option"
                                         ItemTemplate="{Binding Source={StaticResource DataTemplate_NameValue}}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </StackPanel>
</Page>
